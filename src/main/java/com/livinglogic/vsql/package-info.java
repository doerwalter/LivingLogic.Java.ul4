/**
vSQL is a subset of UL4 that can be compiled to SQL expressions.

This makes it possible to generate SQL `select` statements that support arbitrary
field expresssions, `where` condition and `order by` expression.

There SQL statement are save from SQL injection.

vSQL is used in database queries in the
<a href="https://www.living-apps.de/">LivingApps platform</a>.
**/

package com.livinglogic.vsql;
